<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net471</TargetFramework>
    <AssemblyName>KdSoft.Lmdb.Tests</AssemblyName>
    <RootNamespace>KdSoft.Lmdb.Tests</RootNamespace>
    <Platforms>AnyCPU;x64;x86</Platforms>
  </PropertyGroup>

  <PropertyGroup>
    <CodeAnalysisRuleSet>..\Solution.ruleset</CodeAnalysisRuleSet>
    <LangVersion>latest</LangVersion>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="KdSoft.Utils.Portable" Version="3.5.1" />
    <PackageReference Include="Microsoft.CodeAnalysis.FxCopAnalyzers" Version="2.6.0-beta2" />
    <PackageReference Include="Microsoft.DotNet.PlatformAbstractions" Version="2.0.4" />
    <PackageReference Include="Microsoft.Extensions.Configuration" Version="2.0.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="2.0.0" />
    <PackageReference Include="System.Memory" Version="4.5.0-preview1-26101-02" />
    <PackageReference Include="xunit" Version="2.3.1" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.3.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Lmdb\Lmdb.csproj" />
  </ItemGroup>

  <ItemGroup Condition=" '$(OS)' == 'Unix' ">
    <NativeLib Include="../Lmdb/runtimes/osx/native/lmdb.dylib" />
  </ItemGroup>
  <ItemGroup Condition=" '$(Platform)' == 'x86' AND '$(OS)' != 'Unix' ">
    <NativeLib Include="../Lmdb/runtimes/win-x86/native/lmdb.dll" />
  </ItemGroup>
  <ItemGroup Condition=" ('$(Platform)' == 'x64' OR '$(Platform)' == 'AnyCPU') AND '$(OS)' == 'Windows_NT' ">
    <NativeLib Include="../Lmdb/runtimes/win-x64/native/lmdb.dll" />
  </ItemGroup>
  <ItemGroup>
    <Folder Include="TestEnv\" />
  </ItemGroup>

  <Target Name="CopyNativeLib" AfterTargets="Build">
    <Copy SourceFiles="@(NativeLib)" DestinationFolder="$(OutputPath)" />
  </Target>

</Project>
